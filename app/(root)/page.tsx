import Container from '@/components/Container';
import Main from './main/Main';

export default function Home() {
  // const user = useSelector(selectUser);

  return (
    <Container>
      <Main />
    </Container>

    // next.js에서의 slug 개념
    // next.js app dir dynamic
  );
}
